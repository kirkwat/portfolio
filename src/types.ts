export interface TechnologyItem {
  name: string;
  imgSrc: string;
  altSrc: string;
  link: string;
}

export interface FeatureItem {
  description: string;
  icon: string;
  title: string;
}

export interface FooterLink {
  description: string;
  icon: string;
  url: string;
}
